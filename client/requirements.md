## Packages
framer-motion | Complex animations and page transitions
react-icons | Comprehensive icon library for UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Modern, clean icons (already in base, but ensuring availability)

## Notes
- The backend provides a single endpoint POST /api/contact for the contact form.
- All other content (Services, About, Sectors) is static for now.
- Images will be sourced from Unsplash with descriptive keywords.
- Font strategy: 'Outfit' for headings (modern, geometric), 'Inter' for body (clean, legible).
